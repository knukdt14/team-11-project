CV 파이프라인 상세 설계 — 영상 입력부터 과실비율까지
📌 이 문서는 CV 모듈의 기술 레퍼런스입니다. 범위·일정·역할은 README.md가 우선합니다. 두 문서가 충돌하면 README를 따르세요.

지원 범위
고정 카메라(CCTV) 영상 2~3개만. 블랙박스는 지원하지 않습니다 (§S1)
CV의 역할
판정기가 아니라 입력 어댑터 — 후보만 제안, 확정은 사용자가
구현 현황
전부 미구현. 아래 코드는 전부 설계 의사코드입니다
우선순위
웹서비스(README §15 트랙 A) > CV(트랙 B). CV가 전부 실패해도 시연은 됩니다
텍스트 입력 경로는 그대로 유지되고, 영상은 입력 경로가 하나 늘어나는 구조입니다.

0. 설계 원칙 (이것부터 합의)
요약본은 README §4에 있습니다. 아래는 구현자용 상세입니다.

① CV는 판정기가 아니라 입력 어댑터다 영상에서 과실비율을 바로 뽑지 않습니다. CV는 사고유형 후보 + 수정요소 후보를 제안하고, 사용자가 화면에서 확인·수정한 뒤 기존 파이프라인이 계산합니다. → CV가 틀려도 서비스가 무너지지 않습니다. 정확도에 시스템 전체가 인질로 잡히지 않는 구조.

② 숫자는 여전히 apply_modifiers()만 만든다 CV도, LLM도 과실비율 숫자를 만들지 않습니다. 이 원칙은 영상이 들어와도 안 바뀝니다.

③ 모든 추출 특징은 신뢰도를 함께 낸다 신뢰도가 낮은 특징은 자동 적용하지 않고 되묻기 질문으로 승격시킵니다. "신호등이 있는 교차로였나요?" ← CV가 확신 못 한 것이 그대로 질문이 됩니다.

④ 텍스트 경로가 항상 기본, 영상은 선택 경로 시연은 텍스트 경로로도 완주 가능해야 합니다. 영상 처리 실패 시 텍스트 입력으로 안내.

⑤ 추출 결과는 반드시 화면에 그려서 보여준다 ★ 이번 프로젝트에서 가장 중요한 원칙 tracks 배열을 만들고 끝내면 안 됩니다. 바운딩박스·track_id·궤적·충돌지점·특징 배지를 영상 위에 렌더해서 사용자가 눈으로 확인할 수 있어야 합니다. → 그래서 S7(주석 렌더)이 선택 사항이 아니라 필수 단계입니다. 상세는 §S7.

1. 전체 흐름
[사용자] 사고 영상 업로드
    │
    ▼
┌─ CV 모듈 (비동기 작업) ──────────────────────────────────────┐
│  S0. 영상 정규화      디코딩 · 10fps 리샘플 · 리사이즈          │
│  S1. 카메라 유형 판별  블랙박스(ego-motion) vs 고정(CCTV)       │
│  S2. 검출 + 추적      YOLO → ByteTrack → track_id별 궤적        │
│  S3. 궤적 정제        접지점 좌표 · 칼만 평활 · ego-motion 보정  │
│  S4. 충돌 감지        IoU 급증 + 속도 급변 → 충돌 시각 t_c       │
│  S5. 특징 추출        진행방향 · 진입각 · 선진입 · 야간 · 충돌부위 │
│  S6. 사고유형 매핑     규칙 결정트리 → (대, 중, 소) + 신뢰도      │
│  S7. 주석 렌더 ★      주석영상 · 충돌 프레임 3컷 · 궤적 요약 1장   │
└──────────────────────┬───────────────────────────────────────┘
                       │ {사고유형_힌트, 특징[], 신뢰도, 되묻기[],
                       │  주석영상, 프레임[], 궤적요약이미지, 단계로그[]}
                       ▼
┌─ 상담 파이프라인 (트랙 A · CV와 병렬 개발) ──────────────────┐
│  classify_accident  ← CV 결과가 있으면 건너뜀 (힌트 우선)       │
│  search_kb          메타필터 + 벡터/키워드 검색                 │
│  apply_modifiers    ★ 순수 함수 · 과실비율 계산                │
│  lookup_law / get_diagram                                     │
│  LLM answer         근거 기반 설명 생성                        │
└──────────────────────┬───────────────────────────────────────┘
                       ▼
              [대시보드] 게이지 · 워터폴 · 수정요소 토글
                        + 주석 영상(바운딩박스·궤적) 재생
                        + 충돌 순간 프레임 3컷 · 궤적 요약 이미지
                        + 근거 도표 이미지 · PDF 지면 렌더
                        + CV가 확신 못 한 항목은 되묻기 칩으로
2. CV 모듈 상세
S0. 영상 정규화
목표 fps = 10          # 궤적 분석에 충분, 연산량 1/3
목표 해상도 = 720p 이하  # YOLO 입력 640
최대 길이 = 15초        # 충돌 전후 구간만
긴 영상은 충돌 후보 구간만 잘라서 처리 (전체 프레임 추론은 낭비).
프레임 인덱스 ↔ 실제 시각(초) 매핑 테이블을 유지 — 나중에 "충돌 2초 전" 같은 윈도우 계산에 필요.
S1. 카메라 유형 판별 ★ 이 분기가 전체를 가름
블랙박스와 고정 카메라는 좌표계 자체가 다릅니다. 먼저 갈라야 합니다.

# 배경 특징점의 전역 움직임 크기로 판별
pts = cv2.goodFeaturesToTrack(gray_prev, maxCorners=200, qualityLevel=0.01, minDistance=8)
pts = [p for p in pts if not inside_any_bbox(p)]      # 차량 내부 점 제외
nxt, st, _ = cv2.calcOpticalFlowPyrLK(gray_prev, gray_cur, pts, None)

flow = median(norm(nxt[st==1] - pts[st==1]))
카메라유형 = "dashcam" if flow > τ_flow else "fixed"   # τ_flow ≈ 2.0 px @ 10fps
유형	특징	처리
고정(CCTV)	배경 정지, 두 차 모두 화면에 보임	좌표 그대로 사용 — 난이도 낮음
블랙박스	배경이 흐름, 자차가 안 보임	ego-motion 보정 필요 — 난이도 높음
🚫 확정 규칙 — 블랙박스는 지원하지 않습니다
이건 권장이 아니라 범위에서 잘라낸 항목입니다 (README §3). 블랙박스는 자차가 화면에 없어서 "A(나)의 궤적"을 관측하는 것 자체가 불가능합니다. 자차 궤적을 광류로 역산하는 건 별도 연구 주제이고, 이번 모듈 기간에 들어갈 자리가 없습니다.

그럼 S1은 왜 만드나 — "판별해서 처리를 분기"하려는 게 아니라 "거르려고" 만듭니다.

if 카메라유형 == "dashcam":
    return {
      "상태": "unsupported_camera",
      "안내": "블랙박스 영상은 자차가 화면에 없어 궤적을 분석할 수 없습니다. "
              "고정 CCTV 영상을 올리거나, 텍스트로 상황을 입력해 주세요.",
      "축소결과": {야간 여부, 충돌 시점}   # 여기까지만 제공
    }
아래 §S3의 compensate_ego_motion(ego-motion 보정)은 구현하지 마세요. 설계 기록으로만 남깁니다.

