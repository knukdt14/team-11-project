# 과실비율 상담 서비스 — 팀 공통 의존성
#
#   conda create -n team-11-project python=3.11 -y
#   conda activate team-11-project
#   pip install -r requirements.txt
#
# 설치·실행 안내는 QUICKSTART.md 참고.
#
# ─────────────────────────────────────────────────────────────────
# 왜 버전을 == 로 고정하나
#   · chromadb 는 버전이 다르면 커밋된 `vector_index/` 를 못 읽습니다.
#   · 임베딩 모델이 다르면 차원이 달라져 검색이 아예 실패합니다.
#   · 팀원마다 버전이 다르면 "내 PC에서는 되는데"가 반복됩니다.
#
#   아래 버전은 Python 3.11 환경에서 실제로 설치·검증된 조합입니다.
#   함부로 올리지 말고, 올려야 하면 팀에 공유하고 다 같이 올리세요.
# ─────────────────────────────────────────────────────────────────

# ── 데이터 파이프라인 (PDF → JSONL → 청크) · 1번 ──────────────────
pymupdf==1.28.0
pydantic==2.13.4

# ── 벡터 인덱스 · 검색 · 2번 ──────────────────────────────────────
# 현재 hani/data/processed/vector_index 는 이 버전으로 읽힙니다.
chromadb==1.5.9

# 임베딩 모델: BAAI/bge-m3 (1024차원)
# ⚠️ 최초 1회 약 2.2GB를 내려받습니다. 이후로는 캐시를 씁니다.
sentence-transformers==5.6.1
transformers==5.14.1

# ⚠️ CPU 빌드입니다. 팀 전원이 같은 결과를 얻는 쪽을 우선했습니다.
#    NVIDIA GPU가 있으면 아래로 갈아끼우세요 (검색 결과는 동일, 인덱싱만 빨라짐):
#      pip install --index-url https://download.pytorch.org/whl/cu121 torch --force-reinstall
torch==2.13.0

# 키워드(BM25) + 벡터 하이브리드 검색
rank-bm25==0.2.2

# ── 백엔드 · 3번 ─────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# ── 프론트엔드 · 4번 ─────────────────────────────────────────────
streamlit==1.60.0
plotly==6.9.0
requests==2.34.2

# ── 테스트 ───────────────────────────────────────────────────────
pytest==9.1.1
